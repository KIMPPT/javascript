git remote add (특정이름) github주소
만약 잘못 입력한 경우
git remote remove (위에 지정된 특정이름)
을 입력하면 설정이 지워짐

document.querySelector('').innerHTML
문서(document)에서 선택자를 사용하여(querySelector) id 값이 a인 ("#a") "또는 name 값이 b인(".b")태그를 선택하고 HTML에 삽입한다(innerHTML)